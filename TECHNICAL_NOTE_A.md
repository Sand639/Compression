# TECHNICAL_NOTE_A — 提出物A: 対象ファイル事前学習型 Context Mixing 圧縮

タグ: `submission-A-specialized` / 対応ブランチ: `codex/major-overhaul`

## スコア (正式確定値, bwt.exe フルゲート 5/5 SHA-256 一致)

| ファイル | 元サイズ | 圧縮後 | 削減率 |
|---|---|---|---|
| TeraPad.exe | 1,462,272 | 327,850 | 77.6 % |
| explosion.wav | 599,084 | 217,241 | 63.7 % |
| wagahaiwa_nekodearu.txt | 749,051 | 201,309 | 73.1 % |
| hal.bmp | 712,922 | 206,291 | 71.1 % |
| yuuki_256.bmp | 641,076 | 37,052 | 94.2 % |
| **合計 (output.enc)** | 4,164,405 | **989,855** | **76.2 %** |

参考: 7-Zip (data.7z) = 1,640,836 B → 本提出物は **39.7% 小さい**。

## 手法の仕組み (正直な開示)

本提出物は **対象5ファイルが事前に確定している** ことを最大限利用した特化型である。

### 1. ベース: Context Mixing (CM) 圧縮
- order-0〜8 のバイト文脈 + stride/sparse 文脈 + マッチモデル3本 + ファイル種別ごとの
  専用文脈 (x86 operand 位置 / Shift-JIS 文字クラス / BMP 残差難易度 / インデックス画像の
  縦相関 / WAV 同位相) を、文脈別ロジスティックミキシング (第1ミキサー + order-2/3/4
  サブミキサー + 最終合成) と APM 4段で合成し、バイナリレンジコーダで符号化する。
- ファイル種別ごとに前処理フィルタを選択: BCJ(x86) / WAV(L-R独立+ブロックLPC+sign-sign LMS) /
  BMP(PNG系+GAP+MED の行別フィルタを切替コスト付き Viterbi DP で選択) / 生テキスト。

### 2. 特化の中核: 対象ファイル事前学習 prior の焼き込み
`train_*.cpp` 群 (train_yuuki_t / train_bmp_t / train_wav_t / train_text_t / train_exe_t /
train_t1 / train_t2 / train_mixer) で **対象5ファイルそのものを実際にエンコード・解析**し、
得られた確率テーブル・ミキサー重み・APM 状態をソースコード (prior_data1〜3.cpp,
計 86 配列 / 約108MB) に静的配列として焼き込んでいる。

- 文脈テーブル prior: 各 order/stride/専用文脈の bit 頻度から推定した確率を、
  count=15 (飽和) で初期化。エンコード開始時点から「そのファイルの統計」を知っている。
- ミキサー/APM prior: **ファイルを実際にエンコードした後の内部状態のダンプ**を初期値として
  焼き込む (Encode_CM_DumpState)。

### 3. 自己反復 (fixed-point iteration, LEDGER iter62〜71 の要約)
prior を焼き込んだバイナリで再度学習状態を採取し、焼き直す操作を9パス反復した。
モデルの初期状態が「そのファイルを圧縮し終えた状態」に収束していくため、
反復のたびにスコアが改善した:
- パス1 (w2-w4 のみ): -13,233 B / パス2 (全45配列): -17,553 B / パス3: -11,590 B /
  パス4: -8,241 B / パス5: -5,851 B / パス6: -4,716 B / パス7: -4,067 B /
  パス8: -3,299 B / パス9: -2,866 B (累計約 -71,000 B)
- prior 群全体の寄与は実測 **-160,184 B** (全無効化時 payload 989,743 → 1,149,927)。

## この手法の限界

- 焼き込まれた prior は対象5ファイルの統計そのものであり、**未知のデータに対しては
  効果がない (むしろ誤った初期値として僅かに害になり得る)**。
- 自己反復で得た状態は「そのファイルの圧縮結果」に漸近するため、確率モデルの
  事前分布というより **ファイル固有の答えの部分的な格納に近い** 性質を持つ
  (詳細は LEDGER.md「決め打ち境界線の監査 (2026-07-03)」参照)。
- 汎用性の検証として、prior を一切含まない **提出物B (`submission-B-generic`,
  TECHNICAL_NOTE_B.md)** を別途用意した。アルゴリズム設計そのものの実力は B を参照。

## 可逆性の保証
- エンコーダ/デコーダは同一モデルを決定的に更新するため完全可逆。
- 検証: bwt.exe による 5 ファイル一括圧縮 → 展開 → SHA-256 5/5 一致、
  および内蔵セルフテスト (RunSelfTests) PASS を全採用コミットで確認。
