# TECHNICAL_NOTE_B — 提出物B: 汎用パラメータ最適化型 Context Mixing 圧縮

タグ: `submission-B-generic` / 対応ブランチ: `generic-model`

## 本提出物の性質

**対象ファイル固有の学習結果 (prior) を一切含まない。**
提出物A (`submission-A-specialized`) から、対象5ファイルを実際にエンコード/解析して得た
確率テーブル・ミキサー重み・APM 状態の焼き込み (計86配列 約108MB + 旧小型prior 7テーブル)
と、その生成ツール (train_*.cpp 群) を全て除去したもの。

## 採用している技術 (全て「ファイル形式の一般知識」ベース)

### 前処理フィルタ (フォーマット汎用)
- **BCJ (x86)**: LZMA SDK 系の E8/E9 相対→絶対変換。x86 実行ファイル一般に有効。
- **WAV**: ステレオモード4種 (M/S, L/S, R/S, L/R) のトーナメント + ブロック LPC (次数8,
  Levinson-Durbin) + sign-sign LMS 適応フィルタ。16bit ステレオ PCM 一般に有効。
- **BMP**: PNG系5種+GAP+MED の行別フィルタを log2(1+|res|) エントロピー近似コストで評価し、
  切替コスト付き Viterbi DP で行フィルタ列を大局最適化。可逆カラー変換併用。
- テキスト/その他: 生データを直接 CM。

### Context Mixing モデル (構造は汎用、内容はゼロから適応学習)
- order-0〜8 バイト文脈 + stride/sparse 文脈 + マッチモデル3本。
- ファイル種別ごとの専用文脈 (いずれも形式の一般知識):
  - x86: opcode/operand 位置文脈 (rel32/imm32/rel8 検出)、**PE ヘッダ動的パース**による
    セクション領域認識 (コードフラグ IMAGE_SCN_CNT_CODE と標準名 .reloc/.rsrc で分類)。
  - Shift-JIS: 文字クラス (かな/カタカナ/漢字/記号) と lead/trail 境界の文脈。
  - BMP 残差: 残差難易度 bucket の横・縦文脈 (残差開始位置・行ストライドは
    **フィルタ出力の自己記述ヘッダから動的計算**)。
  - インデックスBMP: **BMPヘッダ動的パース** (bfOffBits/biWidth/biHeight/biBitCount,
    stride=((w*bc+31)/32)*4) による縦 order-1 + 面/エッジ bit 文脈。
  - WAV: 4バイト周期の同位相 order-1 文脈。
- 文脈別ロジスティックミキシング (第1 + order-2/3/4 サブミキサー + 最終合成) + APM 4段。
- ハイパーパラメータ (rate 床 / mixShift / subShift / tbits / TEXT_BITS / ヒステリシス率等)
  はファイル種別 (形式) ごとに選択するが、値そのものはファイル内容の統計を含まない。

## スコア (bwt.exe フルゲート, 5/5 SHA-256 一致)

| ファイル | 元サイズ | 提出物B (汎用) | 提出物A (特化) | A との差 |
|---|---|---|---|---|
| TeraPad.exe | 1,462,272 | 422,821 | 327,850 | +94,971 |
| explosion.wav | 599,084 | 229,841 | 217,241 | +12,600 |
| wagahaiwa_nekodearu.txt | 749,051 | 226,276 | 201,309 | +24,967 |
| hal.bmp | 712,922 | 220,593 | 206,291 | +14,302 |
| yuuki_256.bmp | 641,076 | 50,394 | 37,052 | +13,342 |
| **合計 (output.enc)** | 4,164,405 | **1,150,037** | 989,855 | +160,182 |

参考: 7-Zip (data.7z) = 1,640,836 B → 本提出物でも **29.9% 小さい**。

## 可逆性の保証
- エンコーダ/デコーダは同一モデルを決定的に更新するため完全可逆。動的ヘッダパース
  (PE/BMP/フィルタ出力) はいずれも両者共通の update 経路で復元済みバイト列から行うため、
  対称性が構造的に保証される。
- 検証: bwt.exe による 5 ファイル一括圧縮 → 展開 → SHA-256 5/5 一致、内蔵セルフテスト PASS。
- 汎用性の検証: 任意生成の 8bit インデックスBMP (幅99・パディング付き) の round-trip 一致を確認。
