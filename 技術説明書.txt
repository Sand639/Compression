ARC1(AdaptiveRoutingCompressor1/適応型経路選択圧縮機1)説明書

目次
<実行フォルダについて>
<工夫した点>
<各ファイルの採用方式と結果>
<使用したアルゴリズム>
<ソースコードの構成>
<アルゴリズムの説明>

<実行フォルダについて>
1を入力で圧縮
2を入力で展開になっています。
圧縮をするときはフォルダを用意してそのフォルダの中に圧縮したいものをいれて、
一番へのフォルダ名を入力してください
単一ファイルで圧縮するときは拡張子も含めて入力してください

展開時は拡張子を含めなくても展開することができます


<工夫した点>
拡張子ごとに最適そうな圧縮をするのではなく、一つのファイルに登録してある圧縮アルゴリズムを全通り試し、一番小さくなる圧縮アルゴリズムで圧縮をするというトーナメント方式を採用しました。

また、特定のファイルに合わせた決め打ちの値をコードに埋め込むのではなく、ファイル形式の一般知識（PE ヘッダのセクション構造、BMP ヘッダの幅・高さ・ビット深度、Shift-JIS の文字クラスなど）を実行時に動的に解析して予測の文脈に使う構成にしました。このため同じ形式の別ファイルにもそのまま適用できます。

<各ファイルの採用方式と結果>
  ファイル                    元サイズ      圧縮後     採用方式     削減率
  ------------------------- ----------- ----------- ----------- --------
  TeraPad.exe               1,462,272     422,208   BCJ + CM     71.1 %
  explosion.wav               599,084     229,816   WAV + CM     61.6 %
  wagahaiwa_nekodearu.txt     749,051     226,049   CM           69.8 %
  hal.bmp                     712,922     220,491   BMP + CM     69.1 %
  yuuki_256.bmp               641,076      50,294   Index + CM   92.2 %
  ------------------------- ----------- ----------- ----------- --------
  合計                      4,164,405   1,148,970                72.4 %

実行時間 15分程度
(ソリューションのDebugから実行したら1時間以上かかるため注意)
(実行ファイル用の予測モデルが最大16GB程度のメモリを使用するため、32GB RAM環境を推奨)

 ※ いずれも「前処理フィルタ（または専用文脈モデル）でデータの相関を除去 →
     コンテキストミキシング(CM)で符号化」という共通構造になった。yuuki_256.bmp は
     パレット画像のため、BMPヘッダを動的に読み取り「1行上の同位置の画素」を文脈に
     する専用インデックスモデル(Index + CM)が最も小さくなった。

<使用したアルゴリズム>
この圧縮機には19種類の圧縮アルゴリズムが搭載されています

--生データのまま処理する方式--

1.Store(無圧縮格納)
2.RAW（エントロピー直掛け）

--汎用/王道系の圧縮アルゴリズム--

3.BWTパイプライン
4. LZSS単体
5. CM単体（コンテキストミキシング）

--LZSSが圧縮しやすいようにデータを加工してLZSSで圧縮するアルゴリズム--

6. Delta 1 + LZSS
7. Delta 2 + LZSS
8. Delta 3 + LZSS
9. Delta 4 + LZSS
10.BCJ + LZSS 
11.WAV + LZSS
12.BMP + LZSS

--CMが圧縮しやすいようにデータを加工してCMで圧縮するアルゴリズム--
13. BCJ + CM
14. WAV + CM
15. BMP + CM
16. BMP + CM (sep)
17. WAV + CM (位相文脈なし版)
18. Yuuki Index + CM (インデックスBMP向け・位相文脈あり)
19. Index + CM (インデックスBMP向け・ゼロ初期適応)

<ソースコードの構成>
compress.h     共通ヘッダ（型・定数・関数プロトタイプ・直列化ヘルパ）
main.cpp       エントリポイント（圧縮→復元→完全一致検証のドライバ）
transform.cpp  BWT / MTF / Zero-run RLE
huffman.cpp    正準ハフマン
rangecoder.cpp レンジコーダ（0/1/2 次）＋ エントロピー選択
cm.cpp         コンテキストミキシング（中核）
lzss.cpp       LZSS 各変種
filters.cpp    Delta / BCJ / WAV / BMP 予測フィルタ
pipeline.cpp   ブロック分割 BWT パイプライン
io.cpp         ファイル・パス入出力
archive.cpp    ARC1 コンテナ ＋ ファイル単位トーナメント
selftest.cpp   アルゴリズム自己テスト

<アルゴリズムの説明>
各アルゴリズムについて「何をするのか」「なぜ小さくなるのか」「どうやって元に戻すのか（可逆性）」を中心に説明します。


◆ 共通の土台 ── エントロピー符号化とレンジコーダ

すべての方式は最終的に「次に来るデータを予測し、その予測がよく当たるほど短いビット数で書き出す」ことでサイズを縮めています。確率 p で当たる記号は約 -log2(p) ビットで表せる、というのが情報理論（エントロピー）の基本です。

本機ではこれをレンジコーダ（算術符号の一種）で実装しています。[0, 2^32) の数直線（レンジ）を、予測した確率の比に従って「次の記号にあたる区間」へと次々に狭めていき、最後に残った区間を指す数値を出力します。予測が正確なほど区間が広く保たれ、出力ビットが減ります。復号は同じ予測を再現しながら区間を辿り直して記号列を復元します。予測モデルが符号化側と復号側で完全に一致するので、ずれは生じません（完全可逆）。


◆ 1. レンジコーダ（RAW / 0・1・2 次）

入力バイトの出現頻度を学習しながらレンジコーダで符号化する方式です。
  ・0 次 … 直前の文脈を見ず、各バイトの頻度だけで符号化する。
  ・1 次 … 直前 1 バイトを文脈にして、その後に続くバイトの頻度を学習する。
  ・2 次 … 直前 2 バイトを文脈にする。
文脈を長くするほど「この並びの次は何が来やすいか」を細かく当てられますが、文脈表が大きくなり学習も遅くなります。Entropy 段では 0/1/2 次を実際に符号化して最も小さいものを 1 バイトのタグ付きで選びます。RAW 方式はこれを生データへ直接適用するものです。


◆ 2. 正準ハフマン符号

各バイトに「頻度が高いほど短いビット列」を割り当てる古典的な方式です。本機では木構造そのものは保存せず、各記号の符号長（ビット数）だけを保存し、復号側が決められた規則（正準的な順序）で符号を組み立て直します。これで表のサイズを節約しています。また、極端に偏ったデータでも符号長が 15bit を超えないよう制限し、表を一定サイズに保っています。BWT パイプラインの最終段の選択肢として用意しています。


◆ 3. BWT パイプライン

「同じ並びが繰り返し現れる」テキストのようなデータに有効な王道の組み合わせで、次の 4 段を順に通します。
  (1) BWT（Burrows-Wheeler 変換）
      データを巡回させて全ての部分列を辞書順に並べ替え、その「末尾の文字」だけを取り出します。すると同じ文脈に続く文字が一箇所に集まり、同じ文字が長く連続しやすくなります。並べ替えに使った行番号を 1 つ保存しておくことで元に戻せます（接尾辞配列を高速に構築して実現）。
  (2) MTF（Move-To-Front）
      直近に出た文字ほど小さい番号で表す変換。BWT 後は同じ文字が固まっているので、出力は 0 付近の小さな値に偏ります。
  (3) ゼロ連 RLE
      MTF 後に大量に並ぶ 0 を「0 の個数」に置き換えて縮めます（連長は可変長整数で格納）。
  (4) レンジコーダ
      偏った値の列をエントロピー符号化して仕上げます。
各段が完全に可逆なので、逆順にたどれば原文を復元できます。


◆ 4. LZSS

「過去に出てきたのと同じ並び」を見つけ、それを（距離, 長さ）という参照に置き換える辞書式の圧縮です。繰り返しの多いデータを短く表現できます。本機では一致の探し方・並べ方を変えた複数の版を用意し、トーナメントで最小を選びます。
  ・貪欲法 … その場で見つかる最長一致をすぐ採用する。
  ・最適パース … 先のコストまで考えて「どこを参照にすれば全体が最小か」を動的計画法で求める。
  ・split / 4-stream … リテラル・長さ・距離などを種類ごとに別ストリームへ分けて並べ、後段のエントロピー符号化が効きやすい形にする。
後述の前処理フィルタと組み合わせ、フィルタ後のデータを LZSS で圧縮する方式群（Delta+LZSS など）もあります。


◆ 5. CM（コンテキストミキシング）  ★本機の主力

最も強力なバックエンドで、最終的に全 5 ファイルがこの方式（または前処理＋CM）を採用しました。データを 1 ビットずつ符号化し、「次のビットが 1 になる確率」を多数の予測器の意見を統合して求めます。仕組みは次の通りです。

  (1) 多数の文脈モデル（15 個）
      ・order-0〜8 … 直前 0〜8 バイトを文脈にして、次ビットの確率を保持する。
      ・スパース文脈 … 一定間隔で飛ばした位置を文脈にする（テキストは 3、実行ファイルは 4 バイト間隔）。
      ・マッチモデル ×3 … 過去に現れた一致列（4/6/8 バイトのハッシュで検索）をたどり、「前回と同じなら次も同じ」と予測する。一致が長いほど自信を強くする。
      ・x86 オペランド文脈 … 実行ファイルで opcode の種類と即値/相対アドレスの何バイト目かを文脈にする。PE ヘッダを動的に解析し、コード/データ/リソースなどの領域別に order-0 文脈も分離する。
      ・ファイル種別専用文脈 … テキストは Shift-JIS の文字クラス列（ひらがな/カタカナ/漢字/句読点など）、画像は残差の大きさや 1 行上の画素（BMP ヘッダから行幅を動的取得）、音声は 1 サンプル前の同位相バイトを文脈にする。

  (2) 適応カウンタ
      各文脈の確率は（確率, 観測回数）を 1 つの数値に詰めて持ち、観測回数が少ないうちは大きく、増えると小さく更新します（立ち上がりを速く、安定後は緩やかに）。

  (3) ロジスティック混合（2 層ミキサー）
      各予測を「対数オッズ（stretch）」の領域に変換し、重みを掛けて合計します。重みは予測が外れた誤差に比例して逐次更新される（オンライン学習）ので、データに合わせて「よく当たるモデルを重視」するように育ちます。本機では 4〜5 本の小ミキサー（直前バイト・order-2/3/4 文脈、実行ファイルではさらに order-5 文脈）でいったんまとめ、さらにそれらをビット位置などに応じて合成する最終ミキサーを重ねた 2 層構成です。

  (4) APM / SSE（二次推定）4 段
      ミキサーが出した確率を、別の文脈（直前バイト・ビット位置・一致の強さなど）で「実際の当たり方」に合わせて補正します。これを 4 段重ねて精度を高めています。

  (5) 出力
      得られた確率で二値レンジコーダに 1 ビットを書き込みます。

  復号は同じモデルを同じ順番で動かし、符号器の代わりに復号器でビットを読み出すだけなので、まったく同じ予測列が再現され、原データに戻ります。さらに、学習レート・文脈の細かさ・テーブルサイズなどをファイル種別ごとに最適化した「プロファイル」を持ち、テキスト／実行ファイル／音声／画像で個別に調整しています。どのプロファイルを使うかは方式番号から決まるため、可逆性は保たれます。


◆ 6. 前処理フィルタ（CM や LZSS の前にデータを整える）

データ固有の構造を「予測しやすい形」へ可逆変換してから圧縮します。

  ・Delta
      一定間隔で前の値との差分を取ります。少しずつ変化する数値列を 0 付近に寄せ、後段が圧縮しやすくなります。

  ・BCJ（x86 実行ファイル向け）
      CALL/JMP 命令の「相対アドレス」を「絶対アドレス」に書き換えます。同じ関数を呼ぶ命令のバイト列が揃い、後段が捉えやすくなります（TeraPad.exe で採用）。

  ・WAV（音声向け）
      - Mid/Side 変換で左右チャンネルの相関を除去します（M/S・L/S など複数を試し、最小のものを採用）。
      - ブロック単位の線形予測（LPC）で標本値を予測し、残差（予測とのずれ）に変換します。
      - さらに符号 LMS という適応フィルタで、LPC が取りきれない変化を追従して除去します（全演算を 16bit 丸めにして可逆を保証）。
      - 16bit の残差を「各 16bit がまとまって並ぶ」順に並べ替え、CM の多バイト文脈が効くようにします。
      explosion.wav で採用されました。

  ・BMP（画像向け）
      - 各画素を周囲の画素から予測します（PNG の 5 フィルタに加え、JPEG-LS の MED 予測や勾配適応の GAP 予測を行ごとに選択。行ごとの選択は切替コストを含めた動的計画法で最適化）。
      - 色成分間の相関を取る変換（青・赤から緑を引く）も行います。
      - 残差を CM で符号化します（hal.bmp で採用）。「sep」版は色チャンネルを分離してから CM にかける変種です。

  ・インデックス BMP（パレット画像向け）
      - フィルタでの変換は行わず、BMP ヘッダ（データ開始位置・幅・高さ・ビット深度）を動的に読み取り、「1 行上の同位置の画素」「左右・縦方向の平坦さ」を文脈にした専用モデルで生のインデックス列を直接 CM 符号化します。
      - パレット画像はディザにより隣接画素の相関が弱く、行単位（縦方向）の反復構造の方が強いため、この形が最も縮みます（yuuki_256.bmp で採用）。


◆ 7. ARC1 アーカイブとトーナメント

5 つのファイルを 1 つにまとめる入れ物の形式です。先頭に識別子（「ARC」+ フォーマット版数を表す 1 文字）とファイル数を置き、ファイルごとに「名前・採用方式(algo)・元サイズ・圧縮サイズ・圧縮データ」を並べます。サイズや個数などの数値は可変長整数（LEB128）で格納し、小さな値ほど少ないバイト数で書くことでヘッダの無駄を抑えています。

圧縮時は各ファイルに対し搭載した全方式を実際に試し、最も小さくなった方式とそのデータだけを採用・記録します（トーナメント）。復号時は記録された algo を見て、対応する逆変換を 1 通りに適用するので、「サイズ最適化」と「完全可逆」が両立します。

